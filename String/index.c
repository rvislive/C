// scanf() to read a string

#include<stdio.h>

int main() {
    // char name[50];
    //  scanf("%s", &name);
    // printf("Your name is : %s", name);

    // fgets(name, sizeof(name), stdin);
    // printf("name is: ");
    // puts(name);
    char *ptr = "Rakesh Vishwakarma";
    printf("%s", ptr);
    return 0;
}